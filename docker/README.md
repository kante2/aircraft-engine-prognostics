# Docker Development Environment

이 프로젝트는 Docker 기반 개발 환경을 제공합니다. 호스트 시스템 오염 없이 격리된 환경에서 PyTorch 기반 모델 학습이 가능합니다.

## 🏗️ Architecture

```

cd ~/aircraft-engine-prognostics/docker
docker compose up -d --build
